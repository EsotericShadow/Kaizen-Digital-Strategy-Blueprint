import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  output: undefined, // Let Next.js handle output automatically (default is fine for Vercel)
};

export default nextConfig;
